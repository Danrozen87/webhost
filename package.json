{
  "name": "webcontainer-host",
  "version": "1.0.0",
  "description": "Production WebContainer host for AI coding platform",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@webcontainer/api": "^1.6.1"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
